import * as XLSX from 'xlsx';
import type { Tag, Assembly } from '@/types';
export async function exportToExcelWithPricing(tags: Tag[], assemblies: Assembly[], projectName: string): Promise<void> {}
export async function exportProfessionalBOM(pages: any[], tags: Tag[], assemblies: Assembly[], materials: any[], projectName: string): Promise<void> {}
